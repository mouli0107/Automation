---
- name: Install and start Apache on a web server
  hosts: webservers
  become: yes
  tasks:
    - name: Install Apache
      yum:
        name: httpd
        state: present

    - name: Ensure Apache is started
      service:
        name: httpd
        state: started
